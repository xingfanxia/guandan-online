# CLAUDE.md

Guidance for Claude Code (and other AI assistants) working on **guandan-online**.

## Project overview

Real online multiplayer Guandan (掼蛋) — landscape-first web game. 4 / 6 / 8 player rooms, AI bots with multiple difficulties, auto-sort hand, custom rules per room, invite links, real-time play.

Companion to sibling `../guandan-scorer` (in-person scoring/tracking app). This project is the actual playable game; the scorer was scoring-only.

## Current phase

**Backend + all UI tracks (UI-1/2/3/4/5/6/7) + bot dispatch + Easy/Medium AI tier ladder + host-controlled bot fill + TRIBUTE-1 part D auto-tribute + TRIBUTE-1 part E manual tribute dispatch + Vercel project linked + ROOM-2 all 7 boolean rule axes plumbed + production deploy live (both prebuilt + GitHub auto-deploy paths working as of 2026-05-21) + TRIBUTE-2 6P/8P sweep tribute (multi-pair) end-to-end (2026-05-21) + Bobgy WASM decomposer Phase A integrated into Medium tier (2026-05-21) + UI-7 CSS rotate for forced landscape on portrait mobile (2026-05-21) + Vercel route signature fix unblocking all 9 endpoints (2026-05-21) + **2026-06-09 playability audit (commit `c311449`) — first build verified fully playable by actually driving the UI through multiple rounds locally AND on the production henna alias** (fixed: flat-vs-nested move-body wire mismatch, never-emitted snapshot event, handle-normalization drift, stale cross-round currentTurn, unmounted RoundEnd/Victory screens — see § Last updated 2026-06-09).** Seven autonomous sessions on 2026-05-18 took the project from "P0 logic shipped, needs infra" through "9 HTTP/SSE routes live + integration test green" to "React 19 game surface complete end-to-end + production AI Gateway wired through route handlers + manual tribute commands accepted by the dispatcher + Vercel project `panpanmao/guandan-online` linked with Upstash Redis provisioned":

- **NET-1C** — Upstash live impls of `IdempotencyCache` / `EventLog` / `EventBus` (via `RedisLike` contract). `createRealtimeInfra(env)` selects memory or Upstash by env vars.
- **API surface** (9 routes total):
  - `POST   /api/room/create`
  - `GET    /api/room/[code]` — public room view (tokens redacted)
  - `POST   /api/room/[code]/join` — emits `room_joined` event
  - `POST   /api/room/[code]/leave` — emits `room_left` event (no-op when host dissolves room)
  - `POST   /api/room/[code]/start` — host-only game-start (deal + first trick, creates GameSession)
  - `POST   /api/room/[code]/move` — idempotent move dispatch + publishEvent fanout (move_played/passed, trick_won, round_end, game_end)
  - `GET    /api/sse/[roomId]` — Server-Sent Events with 270s rotation
  - `GET    /api/cron/cleanup-rooms` — Bearer-ADMIN_TOKEN cron handler that prunes stale rooms + reconciles ghost index entries
  - `GET    /api/health`
- **Persistence layer**: `lib/storage/{roomStore,roundStore,sessionStore}.ts` — Memory + Upstash impls wired via `RealtimeInfra`. roomStore now maintains a `<prefix>active` set index updated atomically with create/delete; `listCodes()` enumerates active rooms for the cron pass.
- **Event derivation**: `lib/realtime/{buildGameState,deriveMoveEvent,deriveRoundEndEvents,deriveLifecycleEvents,buildLobbyGameState}.ts`. Move handler emits up to 4 events per move (move_played/passed + trick_won + round_end + game_end), each with a sequential version aligned to the per-recipient SSE log seq. Lifecycle events use a shared `RoomState.eventVersion` counter that startGame consumes (`deal.version = room.eventVersion + 1`) so clients can resume across lobby → game with a single Last-Event-ID.
- **Integration test**: `tests/integration/full-game-flow.test.ts` walks create → 3× join (with lifecycle events) → start → SSE-subscribe → play → pass and verifies the full event sequence including lifecycle backlog.
- **UI foundation** (UI-1 + UI-2):
  - `src/styles/{tokens,components,game-table}.css` — design tokens + component primitives (verbatim from `demos/{tokens,shared}.css`) + S03 game-table layout. Side-effect CSS imports declared via `src/types/css.d.ts`.
  - `src/components/{Card,Hand,Trick,Avatar,OrientationLock}.tsx` — pure presentational React 19 primitives + the orientation wrapper.
  - `src/lib/{orientation,sseClient}.ts` — `useOrientation()` / `lockLandscape()` + minimal EventSource wrapper auth'd via `?token=` (EventSource can't set headers).
  - `src/screens/{GameTable4P,RotatePrompt}.tsx` — wireframe S03 made dynamic via a pure `reduceEvent()` reducer (7 server-event kinds covered) + the universal portrait-mobile fallback overlay.
  - Per `docs/research/mobile-landscape-ux.md` § 1.2, the landscape strategy is NATIVE orientation + Android `screen.orientation.lock` + iOS RotatePrompt overlay. NOT CSS-transform-rotate of the game body (would break touch coords, viewport units, virtual keyboard).
- **Lobby + game screens** (UI-3 + UI-4 + UI-5 + UI-6):
  - `src/lib/{api/rooms,identity,router,seating}.ts` — typed API client (`RoomApiError`), localStorage `@handle` + per-room credential store, hash router (`#/`, `#/create`, `#/wait?code=`, `#/table?code=` + legacy `#table=` direct-launch), and 6P/8P clock-position math (partner at 12 o'clock via N/2 offset).
  - `src/screens/{Landing,CreateRoom,Waiting}.tsx` — UI-3 lobby flow. Landing has 3 CTAs + recent-rooms list + sign-in / join-code modals. CreateRoom has segmented mode picker + AI tier chips + 6 rule toggles + spec preview. Waiting polls `GET /api/room/[code]` every 3s, host start button gated on full+lobby, auto-navigates to `#/table?code=` on `phase=in_game`.
  - `src/screens/{TributeModal,RoundEnd,ALevelFinal,Victory}.tsx` — UI-4/5. TributeModal covers all 4 substates (auto / pending / anti-tribute / return-pending) per demos S04/S11/S12/S13. RoundEnd shows S06 headline + 13-rung `LevelLadder` + 4-col detail strip. ALevelFinal wraps the active table in a warm-red tinted container with a pinned strict-mode A-fail counter (S07). Victory: gold-tinted celebration with 胜 rune + winning roster + optional MVP + share/return CTAs.
  - `src/screens/GameTableMP.tsx` — UI-6 shared 6P/8P table. Same SSE wiring + reducer pattern as GameTable4P, oval-felt layout with clock-positioned opponent seats + 4-team ring color classes (A/B/C/D). `App.tsx` adds a `TableSwitch` wrapper that fetches the room once to dispatch to 4P vs MP.
  - `src/styles/{screens,tribute,round-end,multi-table}.css` — per-surface styles, ported from demos. All imported by `src/main.tsx`.
- **AI tier ladder — Easy + Medium only (v1)**:
  - `lib/ai/easy.ts` (existing) — rule-based + 30% random noise.
  - `lib/ai/{coop,medium}.ts` — partner-cooperation primitives (`decidePartnerCoop` returns `defer`/`cover`/`compete`; `rankByCoop` filters legal plays by advice) and the Medium tier dispatcher (no noise, cheapest legal beat in current coop mode, endgame-trumps-deference).
  - `lib/ai/dispatch.ts` — single `computeBotMove(ctx)` entry point mapping `tier: 'easy' | 'medium'` to the right strategy.
  - **LLM Hard tier deleted 2026-05-19** (see HANDOFF.md 2026-05-19 entry). Reasons: (a) research direction was algorithmic / NN, not LLM (see `docs/research/ai-strategies.md`); (b) LLM latency 1.5-3s/move × multiple Hard bots is structurally incompatible with real-time card play. Hard tier returns post-WASM via the Bobgy `poker-guandan-strategy` solver with deeper search depth — same engine, depth params produce tier separation. DanLM Master tier still deferred to v1.1.
- **In-handler bot run-loop**:
  - `lib/ai/runBots.ts` — synchronous `runBots()` loop wired into both `lib/api/move.ts` (after human's move applies) and `lib/api/startGame.ts` (on game-start, defensive against future bot-leader scenarios). While `round.currentTrick.currentPlayer` is a member with `status==='bot'`, computes + applies the bot's move via `playCards`/`pass`, derives events through `deriveMoveEvent`, appends with sequential versions. Auto-calls `startTrick` between tricks. Round-end fanout (round_end/game_end) operates on the post-bot `advancedRound`.
- **Host-controlled bot fill** (commit `8d5142c`):
  - `lib/room/lifecycle.ts addBotToRoom` — pure transition appending a member with `status='bot'` + `difficulty`. Same capacity + handle-collision checks as `joinRoom`.
  - `lib/api/createRoom.ts` accepts `bots: [{tier: 'easy'|'medium'}]` in the request body. Iterates after host creation, seats each via `addBotToRoom`. Handles drawn from `BOT_HANDLE_POOL` with per-room uniqueness retry. Capacity validated: 1 host + N bots ≤ `positionCount(mode)`.
  - `lib/api/startGame.ts` runs `runBots` after the deal-event publish. Defensive — the canonical case (host = seat 0 = leader) returns early; the branch fires when future leader-selection logic (e.g., tribute-driven leader in subsequent rounds, handled via `dealNextRound.firstLeader`) puts a bot at seat 0.
  - Client wiring: `src/lib/api/rooms.ts createRoom` accepts `bots?: BotSeat[]`; `src/screens/CreateRoom.tsx` submit converts the `aiTiers` chip state into the array (filters out 'human' chips); `src/screens/Waiting.tsx` renders bot rows with tier-emoji avatars + AI tier chips.
- **TRIBUTE-1 part D — auto-tribute + multi-round dealing** (commit `52ea2d3`):
  - `lib/game/nextRound.ts dealNextRound` — pure orchestrator that composes shuffle → deal → detect tribute mode (4P only via `detectTributeMode4P`) → `applyTribute` → `startTrick` into one step. 6P/8P skips tribute and seats 1st place as leader (sweep-tribute deferred per `docs/research/game-rules.md`).
  - `lib/realtime/deriveTributeEvents.ts` — builds `AuthorEvent[]` for the tribute phase. tribute_pending carries direction + obligations + per-recipient `owedCard` via `privatePayloads`. tribute_resolved flattens each `TributeExchange` into 2 wire entries (tribute + return); skipped entirely for 'resist' / 'none'. Returns just tribute_pending(direction='anti_tribute', obligations=[]) for resist.
  - `lib/api/move.ts` wires the helpers after `round_end` fanout when session continues. Critical: cross-round gameState split — events emitted BEFORE `dealNextRound` use the OLD round's gameState; events emitted AFTER use the NEW round's. Closes a leak-detector false-positive where the new shuffle redeals a played card to a different player. Tracked via `newRoundEventStart` index + `preNextRound` snapshot.
  - `lib/realtime/buildClientPayload.ts assertNoOpponentHandLeak` whitelists `tribute_pending` + `tribute_resolved` (semantically public by game rules; tribute_pending per-recipient privatePayloads filtering still happens in buildClientPayload itself). Manual `tribute_select` / `anti_tribute` commands still return `invalid_move` from `handleMoveCommand`; AUTO path only.
- **Upstash env aliasing** (commit `99dd52e`):
  - `lib/realtime/infra.ts RealtimeEnv` accepts both `UPSTASH_REDIS_REST_{URL,TOKEN}` (the @upstash/redis docs convention) and `KV_REST_API_{URL,TOKEN}` (what the Vercel Marketplace integration auto-provisions). UPSTASH-prefixed wins when both pairs are present. `RealtimeInfra.redis` now exposes the underlying `RedisLike` (or null on memory backend) so adjacent clients build without re-reading env. `.env.example` documents both naming pairs.
- **TRIBUTE-1 part E — manual tribute commands** (commit `df6ba3f`):
  - `lib/game/round.ts` gains optional `pendingTribute?: PendingTributeState` on `GameRound`, carrying the detected mode + obligations (with `selectedCard: Card | null`) + finishOrder snapshot. Set during manual-mode tribute phase; cleared by the flow helpers when finalization runs.
  - `lib/game/tributeFlow.ts` is the manual-flow module (sister to `tribute.ts` which owns mode detection + the auto swap). Two pure transitions: `selectTributeCard(round, playerId, card)` validates ownership + card presence + wildcard exemption + no double-select, stashes the card, finalizes via `applyTribute` + `startTrick` when all obligations satisfied. `declareAntiTribute(round, playerId)` validates resist mode + player on losing team; finalizes with no swap and 1st-place as leader.
  - `lib/game/tribute.ts TributeMode` gains optional `tributeCard?: Card` per obligation. `applyTribute` uses the explicit card when provided (validates in-hand + non-wildcard) and falls back to `pickTributeCard` otherwise. Backward-compatible — auto-mode callers (`dealNextRound`) pass mode without the field.
  - `lib/realtime/handleMove.ts` routes `tribute_select` / `anti_tribute` ahead of the trick-based authority check (tribute commands operate on the pre-trick `pendingTribute` state where `round.currentTrick` is null by design). Errors from the flow helpers wrap into `invalid_move` MoveResponse with descriptive details.
  - **Auto-mode at round-start (the current `dealNextRound` default) is unchanged** — it never sets `pendingTribute`, so the new dispatch returns `invalid_move` on rounds that opted into AUTO. Manual-mode round-start wiring (per-room rule, server-side event emission, UI hookup) is a separate concern; this commit lands the handler + state machine that enables it.
- **UI-7 CSS rotate for forced landscape** (commit `2cc6fce`, shipped 2026-05-21; autofocus polish in `0093e20`):
  - `src/components/OrientationLock.tsx` — when device is portrait-mobile, wrap children in a `.orientation-rotate-active` div rotated 90° via CSS (`width: 100dvh`, `height: 100dvw`, `transform: translate(-50%,-50%) rotate(90deg)`). When a text input gains focus, swap to `.orientation-rotate-bypass` (no rotate, input stays mounted — unmounting closes the iOS keyboard immediately).
  - `tokens.css` declares `--vp-h: 100dvh` at `:root`; `.orientation-rotate-active` overrides to `100%`. The 6 existing `100dvh` declarations in `multi-table.css` / `screens.css` / `round-end.css` migrated to `var(--vp-h, 100dvh)` so they size to the rotated wrapper instead of the un-rotated viewport (which would overflow).
  - Per `docs/research/mobile-landscape-ux.md` § Update 2026-05-16 (which overrode the earlier § 1.2 conclusion): modern iOS Safari 16+ and Chrome 90+ correctly translate pointer events through CSS transform. Majsoul / 4399 / WeChat mini-games all ship this pattern in production. The three "hard problems" from § 1.2 (touch coords, viewport units, virtual keyboard) are all tractable for static-layout DOM-based card games like ours.
  - `RotatePrompt.tsx` retained as emergency-only fallback (not wired to OrientationLock currently).
  - Landing autofocus polish: `signInOpen` is `false | 'auto' | 'manual'`. Auto-open useEffect sets 'auto' (no autofocus → rotate stays visible). Header sign-in click sets 'manual' (autofocus on → typing immediate). Critical because the auto-open path always fires on first portrait-mobile paint (no @handle stored) — without this differentiation, the rotate was invisible on first paint.
- **Vercel route signature fix — named HTTP method exports** (commit `cf347e8`, shipped 2026-05-21):
  - All 9 `api/*.ts` routes migrated from `export default async function handler(req: Request): Promise<Response>` to named HTTP method exports: `export async function POST(request: Request): Promise<Response>` (or `GET` for read endpoints). Vercel runtime treats default exports as Express-style `(req, res) => void` and silently ignores returned `Promise<Response>` — request hangs until 300s timeout. Production logs showed `WARN: default export returned a Response. The default-export signature is (req, res) => void — returns are ignored`.
  - Cron broke separately on `request.headers.get is not a function` — same root cause (default-export fallback passed a Node IncomingMessage instead of a Web Request). Named-export pattern fixes both.
  - Local verification via `vercel dev` returned 201 in 519ms for the exact create-room payload that was hanging in production (curl + status check post-deploy: `dpl_7fqaoqai0` Ready in 16s).
  - **Future routes MUST use named HTTP method exports.** `export default async function handler` is the trap.
- **Bobgy WASM decomposer — Phase A** (commit `8a41c83`, shipped 2026-05-21):
  - `lib/ai/decomposer/cpp/` — vendored from upstream `Bobgy/poker-guandan-strategy` (MIT © 2018 Yuan Gong). Two `// PATCH:` markers: flattened `cc/common.hpp` include + added `#include <cassert>` for newer emsdk libc++.
  - `lib/ai/decomposer/dist/{strategy.js,strategy.wasm}` — prebuilt WASM artifact (~248KB total), committed via `.gitignore` `!lib/ai/decomposer/dist/**` exception. Built via `scripts/build-wasm.sh` (Docker `emscripten/emsdk:latest`, only re-run when C++ source changes). Flags: `MODULARIZE=1 + EXPORT_ES6=1 + ENVIRONMENT='node,web' + ALLOW_MEMORY_GROWTH=1` + `EXPORTED_RUNTIME_METHODS=["cwrap","ccall"]` (note: `EXTRA_EXPORTED_RUNTIME_METHODS` is deprecated in current Emscripten).
  - `lib/ai/decomposer/{encode,decode,loader,index}.ts` — TS wrapper. Public API: `decomposeHand(hand, levelRank)` (sync, auto-loads WASM on first call) + `preloadDecomposer()` (optional). **Wire format gotcha**: Bobgy's input is **packed 2-char pairs with NO separator** (`parseCardState` walks `cards[i*2]/cards[i*2+1]`) — only the OUTPUT solutions use spaces and pipes (`| 3H 3S || KC KD |`). Encode joins with `''`; decode splits by `|` and filters empty sections.
  - `lib/ai/medium.ts` — calls `decomposeHand` and, when the suggested first play matches an enumerated legal response (multiset equality on suit+rank+deck), plays it; otherwise falls back to existing `rankByCoop` heuristic. Endgame finisher + defer cooperation policy still take priority over decomposer (it doesn't model trick context or partner). Three fallback paths: (a) WASM not loaded yet, (b) decomposer returns null on wildcard substitution, (c) decomposer dumps the whole hand as one section (Bobgy's solver fallback when it can't decompose further — not a valid Guandan pattern, so no match).
  - Phase B (lookahead policy → Hard tier revival + UI chip) is a separate future session — see `docs/plan/bobgy/PHASE-A.md §10`.

Remaining substantive work: **DNS records for `gdo.ax0x.ai`** at the user's DNS provider — TXT `_vercel.ax0x.ai` = `vc-domain-verify=gdo.ax0x.ai,1a52d460b7c82b46283d` (one-time verification) + CNAME `gdo.ax0x.ai` → `cname.vercel-dns.com` (production traffic) — then end-to-end HTTP smoke can run; **Bobgy WASM Phase B** (lookahead policy on top of the Phase A decomposer + Hard tier revival + UI chip — separate session, materially harder than Phase A; see [`docs/plan/bobgy/PHASE-A.md`](docs/plan/bobgy/PHASE-A.md) §10). **Bobgy Phase A shipped 2026-05-21 (commit `8a41c83`)** in a single autonomous /goal loop — full vendor + build + TS wrapper + Medium integration + 29 new tests. **TRIBUTE-2 6P/8P sweep tribute shipped 2026-05-21 (commit `434c595`)** — was deferred in HANDOFF; closed in a single autonomous /goal loop. **GitHub auto-deploy path was fixed 2026-05-21 (commit `edeb6c7`)** — migrated 275 relative TS imports to `.js` suffixes (so the cloud per-function tsc check accepts them under its default `nodenext` moduleResolution) and added `compilerOptions` to the root `tsconfig.json` so `@vercel/node` walks up and sees `strict: true` + `types: ["node"]` (previously only the references file existed, so it fell back to lenient defaults that broke discriminated-union narrowing in `lib/api/createRoom.ts:65` and lost `process` globals across cron + room routes). `vercel build` locally now emits zero TS errors; the post-commit GitHub auto-deploy completed in 16s with status Ready (`dpl_2frvb1rst`); `/api/health` responds in 298ms behind the SSO 401 page (was hanging with FUNCTION_INVOCATION_TIMEOUT before). AUTH-2 sibling KV migration is **cancelled** as of 2026-05-19 — guandan-online runs on its own Upstash instance; no shared key space with sibling scorer.

See `HANDOFF.md` for the per-commit map and `docs/plan/PLAN.md` for the full 31-milestone roadmap.

The plan has 6 phases (P0 → P5) plus deferred polish. Each phase ends with a working, demoable artifact. See `docs/plan/README.md` for the dependency graph and phase entry criteria.

## Coding conventions (post-bootstrap)

- **TypeScript strict mode** with `noUncheckedIndexedAccess`. Indexing returns `T | undefined` — use `?? fallback` or `!` (with proof of bounds).
- **Pure functions where the sibling has singletons.** Sibling scorer uses a `state` singleton (`src/core/state.js`); online's multiplayer model demands purity. Caller passes state in, function returns a diff.
- **`// SYNC:` pins** — any file that ports sibling-scorer logic carries a `// SYNC: ../guandan-scorer/<path>:<lines>` comment. If sibling changes, update both within the same PR. Drift here breaks cross-app account/result correctness once AUTH-2 ships.
- **Path aliases**: `@/*` → `src/*`, `@lib/*` → `lib/*`, `@tests/*` → `tests/*`.
- **TDD non-negotiable for `lib/game/*` and `lib/realtime/*`** — failing test first, then minimal impl. Other surfaces (UI, scripts) can write tests alongside.
- **Coverage gate**: per-path via `vitest run --coverage` — `lib/**` at 80% (the game/realtime core; CORE-1 targets 95%+), `src/**` ratchet-gated at 74/65/67/72 (React surfaces; recalibrated after the GameTable UI-integration pass — SSE-bound submit glue is brittle to unit-test). Current suite: **1512 tests** as of 2026-05-28 (1138 → 1512 across the v1-completion loop: all 7 remaining milestones + the F1/F2/F13 hardening fixes). Component tests use `// @vitest-environment jsdom` per-file directive + `tests/setup/jest-dom.ts` setup file.
- **Relative TS imports MUST end in `.js`** (e.g. `from './foo.js'` for `foo.ts`). Vercel's per-function tsc check runs under `nodenext` moduleResolution and rejects extensionless imports with TS2835. Our tsconfig uses `moduleResolution: "bundler"` so Vite/Vitest transparently map `.js` → `.ts` at module-resolution time — both paths work. Path-alias imports (`@/foo`, `@lib/bar`, `@tests/baz`) and package imports stay unchanged. Migration applied 2026-05-21 via `scripts/migrations/add-js-extensions.py` (idempotent — re-run safely after adding new code if you forget the suffix).
- **Single publish gateway**: every server→client event MUST route through `lib/realtime/publish.ts` (the only file allowed to call `EventBus.publish` / `EventLog.append`). `scripts/security/grep-no-leak.sh` enforces this at CI time — it fails the build if any file under `lib/` / `src/` / `api/` (except the gateway + bus/log impls + redisClient type defs + tests) directly invokes `.publish(` / `.append(` / `redis.publish(` / `xadd(`. Run via `npm run security:no-leak`.
- **Per-recipient EventLog keys** (security-critical): each filtered payload is appended to `eventLogKey(roomId, recipient)`, NOT to a single per-room key. A shared per-room log mixing all recipients' filtered payloads would let SSE backlog replay surface another player's `yourHand` view on resume. The live bus channels (`game:<roomId>:player:<id>`) already had this isolation; SEC-2 (commit `75424ee`) closed the matching gap on the durable log path. Anyone reading the log MUST use `eventLogKey(roomId, playerId)` from `lib/realtime/publish.ts`.
- **Event version monotonic per-recipient**: SSE `id:` line uses `event.version` (formatted in `lib/realtime/sse.ts`). For client `Last-Event-ID` resume to work, `event.version` must align with the per-recipient log seq. A single move can emit multiple events (e.g., `move_played` then `trick_won`); each gets a sequential version, and the move handler advances `RoundEnvelope.version` to the LAST emitted event so the next move's `fromVersion` check stays aligned. Adding new event-emitting paths MUST preserve this contract — see `lib/realtime/deriveMoveEvent.ts` + `lib/api/move.ts` for the pattern.
- **Comments**: explain WHY not WHAT. The plan's "no narration / no edit-history" rule applies here too.

## Tooling

- Node `>=22` (Vercel deploys on `nodejs22.x`); local dev tolerates Node 23.x with a benign vitest engine warning.
- `npm test` (vitest), `npm run typecheck`, `npm run build` (tsc -b + vite build), `npm run dev` (vite dev server on :5174).
- Vercel project `panpanmao/guandan-online` linked (projectId `prj_Y3gwNGDixTDz5KBfkjJjsYWcwrlv`, GitHub auto-connected). Upstash Redis provisioned via Marketplace integration. Production deploy live both via GitHub auto-deploy (since 2026-05-21, commit `edeb6c7`) and `vercel deploy --prebuilt --prod` (since 2026-05-19); the `gdo.ax0x.ai` custom domain is still pending DNS (add it via the Vercel dashboard to surface its domain-specific CNAME + `vc-domain-verify=…,dc` TXT — CLI `vercel domains add` is blocked, `domain_not_owned`, because the `ax0x.ai` apex is third-party-managed). **Note:** despite the `all_except_custom_domains` SSO setting, the production `*.vercel.app` aliases (e.g. `guandan-online-henna.vercel.app`) are in practice publicly reachable — AX plays on the henna alias and unauthenticated `POST /api/room/*` + SSE work — so DNS is only about the pretty domain, not basic playability. Local dev: `vercel env pull .env.local` preferred over hand-editing.

## Domain references

- **Sibling rule engine** — `../guandan-scorer/src/game/` (calculator.js, rules.js) has working A-level / 4-6-8 mode / upgrade logic. Reuse where possible.
- **Existing scorer themes** — sibling has 5 production themes (broadcast / linear / trading / atelier / teatable) with proven visual tokens. May or may not transplant; the game UI has very different needs from a scorer.
- **Research findings** — see `docs/research/README.md` for the index.

## What this app is NOT

- Not a fork or rewrite of guandan-scorer. Scorer continues to exist for in-person scoring.
- Not a single-device pass-and-play game. This is real online multiplayer.
- Not portrait-mode. Landscape only on mobile (forced via CSS + orientation prompt fallback).

## Anti-patterns to avoid

- Premature architecture choices before research completes
- Cloning sibling project's structure wholesale — different problem domain
- Stub AI bots that play randomly — the brief explicitly asks for **different difficulties**, so AI quality matters

## Layout conventions

Follow the global file-organization rules from `~/.claude/CLAUDE.md`:
- Docs → `docs/<topic>/`
- Adhoc scripts → `scripts/<topic>/`
- No flat dumps at repo root

## Last updated

- 2026-06-09 second pass (bot-response latency 3-5s → ~0.3s, commits `be2d52b` + `1f5c5cb`) — AX: "3-5s for bot is too slow". Measured the production POSTs directly: `/move` took **11.5s**, `/start` **4.1s** server-side. Two stacked root causes: (1) **Region mismatch** — the Vercel Marketplace API (`/v1/storage/stores`) revealed `guandan-online-db` (Upstash) lives in **hnd1 (Tokyo)** while functions ran in default iad1 (DC); every Redis REST call paid ~200-250ms cross-Pacific (verified: `/api/health` ~150ms vs `/api/room/[code]` ~400ms = one GET ≈ +250ms). `vercel.json` now pins `"regions": ["hnd1"]` — also the right region for China-based players. NOTE: nearly all panpanmao Upstash DBs are hnd1; any future Vercel project on this team should pin functions to hnd1 from day one. (2) **Serial publish fanout** — `publishEvent` awaited XADD+EXPIRE on log AND bus per recipient sequentially = 16 serial round-trips per event, 60-100 per move burst. Now: recipients fan out via `Promise.all` (per-recipient append-before-publish order preserved — it guards the SSE subscribe/drain race; and callers await each event sequentially so per-recipient version-ordered XADDs never race), TTL-refresh EXPIREs are fire-and-forget with error logging. **Review follow-up shipped** (`1f5c5cb`, code-reviewer MEDIUM): with best-effort EXPIREs, a dropped FIRST expire on a new stream = key with no TTL, and the cleanup cron only deleted room records — so the cron's stale branch now purges every member's `events:`/`bus:` streams via new `lib/realtime/streamPurge.ts` (key formats exported from their owning modules: `DEFAULT_EVENT_LOG_PREFIX`, `DEFAULT_EVENT_BUS_PREFIX`, `playerChannel`). **Measured after deploy**: `/start` 0.59s, `/move` (incl. 3 bot moves + fanout) 1.06s, and in-browser on the henna alias **click → all 3 bots played → turn back to me = 326ms** (was 3-5s+). 1549 tests (+5 streamPurge) · typecheck · grep-no-leak · CI green.
- 2026-06-09 (playability audit — game made ACTUALLY playable end-to-end, commit `c311449`) — AX reported the game "still completely unplayable with tons of issues". Audited by **playing the game in a real browser** (chrome-devtools MCP driving create → start → play vs 3 bots), which surfaced five root causes no prior test had caught — all client/server CONTRACT or wiring gaps invisible to layer-local suites: (1) **Move wire mismatch (total brick)** — both table screens POSTed the command FLAT (`{kind,...,moveId}`) while `lib/api/move.ts parseMoveBody` requires NESTED (`{moveId, command:{...}}`); every UI play/pass failed `invalid_move` AND the `{ok:false}` HTTP-200 response was silently dropped, so the table just froze. e2e "passed" because test helpers POST the nested shape directly, bypassing the UI client. Fix: shared `src/lib/api/moveClient.ts` (both screens import it; rejection → on-table `move-error` line with zh reasons) + `tests/contract/move-wire.test.ts` round-trips the client builder through the exported server parser. (2) **`snapshot` never emitted** — the wire type existed but NO server code sent it; clients never learned roster/own-playerId/current-turn. Host-vs-bots rooms (the primary solo mode) rendered ZERO opponents (bots never emit `room_joined`) and `myPlayerId` stayed null forever (turn gating + own-hand-shrink + selection-clear all dead). Fix: `handleSse` synthesizes a per-recipient snapshot at stream open from roundStore+sessionStore (new optional `SseDeps`), version = envelope.version, emitted WITHOUT `id:` line (Last-Event-ID safety, same rationale as `stream_closing`), filtered through `buildClientPayload` + `assertNoOpponentHandLeak`, wrapped so a failure can't brick the stream. (3) **Handle normalization drift** — server `lib/auth/handle.ts` strips `@`+lowercases, client `identity.ts` ADDS `@` (the "kept in sync" comment was false); `splitSeats` raw `===` never found the local player. Fix: locate by server-assigned `myPlayerId` first, fallback `handlesEqual()` (normalized compare, new in identity.ts). (4) **Stale `currentTurn` across rounds (soft-lock)** — `DealEvent` carried no leader; after every round transition the new leader's buttons stayed disabled (server said `currentTurn: p0`, client had last round's stale value). Fix: `deal.leader` added end-to-end (AuthorDealEvent → filter → reducers). (5) **RoundEnd/Victory never mounted** — the UI-5 screens existed with tests but no reducer case drove them; `game_end` wasn't even handled (dead table at game end, zero feedback). Fix: `roundEndView`/`gameEndView` in both tables' TableState + overlays (RoundEnd auto-dismisses after 8s — next round is already dealt underneath) + client-side `finishOrder` tracking from handCount hitting 0 (round_end only names the team). **Plus**: `.table` height chain fix (`height:100%` collapsed → black void under the hand on anything taller than ~430px; now `var(--vp-h,100dvh)` honoring the UI-7 rotate wrapper); **snapshotVersion skip-guard** (backlog events ≤ snapshot version are baked into the snapshot; re-reducing them double-decremented every hand count on each reload — modal-state events `tribute_pending`/`exchange_*_required` exempt since the snapshot doesn't carry modal state); snapshot rebuilds mid-trick `lastPlayed` (with client-side `analyzeHand` label) so reloads keep the trick; `reduceDeal` resets counts from `publicHandCounts`; turn flag `▶ 轮到你出牌` + partner-avatar active pulse (there was previously NO whose-turn indication anywhere); pattern labels localized at the render boundary (`Trick.tsx localizePatternLabel`: FLUSHSTRAIGHT→同花顺 etc); `setVersion` max-guard (snapshot arrives at CURRENT version, then backlog replays older ones — `fromVersion` must not regress). **Verified by actually playing**: multiple full rounds vs bots locally (desktop 1280×800 + landscape 844×390; lead/follow/pass, wildcard confirm dialog, trick_won, RoundEnd overlay with ladder, auto-tribute into round 2+ with correct leader carried to LV 8 over several rounds) AND **live on production** (henna alias, room `E4W6H2`: snapshot roster rendered, play applied 27→26, bots cycled, pass + trick rotation worked; live-event latency ~3-5s on the Upstash bus tick — screenshot at `docs/reports/playability-audit-2026-06-09/`). `npm test` **1544/1544** (+21: contract + sseConnectSnapshot + GameTable4P-playability-fixes) · typecheck clean · grep-no-leak green · build clean · CI green on push (`27245331474`, 1m14s) · deploy `nn8ek3erh` ● Ready. **Lesson (extends 2026-05-29's)**: layer-local green is structurally blind to cross-layer contract drift — the move-body shape, the snapshot's existence, and the handle normalization each had BOTH sides individually tested and mutually incompatible. The fix pattern is contract tests that import BOTH sides (see `tests/contract/`) plus the standing rule: before any "playable" claim, drive the real UI through a full round.
- 2026-05-29 (production SSE black-screen — root-caused + fixed, commit `9967a5b`) — **The game was UNPLAYABLE in production**: starting a game rendered a black table with 0 cards / no opponent seats, despite the deploy being Ready, CI green, and `/api/health` returning fast. AX reported it with a screenshot. **This is exactly the gap the prior "v1 done / CI green / deploy verified" claims missed — nobody had ever actually played a game on the production deploy.** Reproduced via curl against `guandan-online-henna.vercel.app` (the public alias AX plays on — unauthenticated `POST /api/room/create` + `/start` + `GET /api/sse/[code]` all work; SSO does NOT block this alias, so the app has been publicly reachable all along): `/start` emitted `version:8`, but the SSE stream returned **HTTP 200 then closed with 0 bytes**. Vercel runtime logs gave the trace: `SyntaxError: "[object Object]" is not valid JSON at Object.range (lib/realtime/eventLog.ts:149) at async Object.start (lib/api/sse.ts:161)`. **Root cause**: `@upstash/redis` ships `automaticDeserialization: true` and JSON-parses Redis Stream field values on read (same as `get`). `eventLog.range` + `eventBus.tick` wrote `JSON.stringify(event)` then *also* called `JSON.parse` on read — but real Upstash had already parsed it to an object, so `JSON.parse(object)` threw. In `range` the throw escaped the SSE `ReadableStream.start` callback → empty stream → client never received the `deal` event → black table; in `tick` the existing catch swallowed it → every LIVE event silently dropped. **Why tests passed**: the test fake `_fakeRedis.xrange` returned raw strings (matching the old `RedisLike` `string` type), so `JSON.parse(string)` worked in tests while `JSON.parse(object)` only threw against real Upstash. **Fix**: `decodeStreamValue()` helper in `lib/realtime/redisClient.ts` (parse strings, pass objects through); `eventLog.range` + `eventBus.tick` use it; `range` adds a per-entry catch so one corrupt record can't brick the whole replay; `xrange` field-value type widened `string`→`unknown` (honest type forces the decode at call sites); `_fakeRedis.xrange` now models auto-deserialization (verified byte-for-byte against `node_modules/@upstash/redis` `deserialize6`) so the existing Upstash log/bus suites become the regression guard; +3 explicit regression tests. **Verified live on production after deploy** (`m2hzzm6v7` ● Ready, henna alias): the same repro now streams the `deal` event and the host receives **13 cards** (correct 8P hand size per `lib/game/cards.ts:79`). `npm test` 1523/1523 · typecheck clean · grep-no-leak green · build clean · code-reviewer APPROVED. **Lesson for future sessions: CI-green + a 200/401 health check ≠ production-verified. Before declaring any game-playable milestone "done", actually create+start+stream one game on the live deploy** — the unit/e2e suites run against memory infra or the vite-api-plugin, never the real Upstash read path that broke here. (The 2026-05-22 audit's "SSE named-event dispatch" fix was a *different* SSE bug one layer up in the browser client; this one is the Upstash deserialization layer in the server read path.)
- 2026-05-28 (v1 ship — push to origin/main + production deploy verification + CI benchmark seed fix) — The v1-completion loop's 18 commits were **local-only**; GitHub auto-deploy had been serving the old `a96ebd8` (Vercel-route-fix + UI-7). This session pushed them (`a96ebd8..b214942`) so v1 is actually on origin/main + deployed. **Production deploy verified**: `hr5i5vmuw` ● Ready; `/api/health` returns 401 (team-SSO gate) in **0.37s** — fast return, not the old `FUNCTION_INVOCATION_TIMEOUT`, so the named-export route signatures genuinely invoke. **First CI run failed** on `tests/ai/strength.test.ts:66` (`hard is at least medium-class`) hitting the 30s per-test timeout — this was the **first time strength.test.ts ran on CI** (the v1 work had never been pushed, so CI never exercised it). **Root cause**: the `hard vs medium` matchup is uniquely expensive — both players lean on the Bobgy WASM decomposer (Hard decomposes per candidate beat for 1-ply lookahead, Medium for lead plays), so it's ~16x the per-game cost of a Medium matchup → 10.8s local / ~32s on the 2-core GitHub runner. **Fix** (commit `11f740c`): split the benchmark's seed budget — the two Hard matchups get a dedicated `BENCH_HARD_SEEDS` (default 30 = 60 games, ample for their loose `≥0.45` / `>0.6` honesty gates) + a 60s timeout (~3.7x headroom over the ~16s CI cost); the precision-sensitive matchups (medium must clear `>0.5`) keep the full `SEEDS=60` (they cost <700ms each regardless). The harness is **deterministic seeded self-play**, so local pass = CI pass; full-fidelity run still available via `BENCH_HARD_SEEDS=60` / `BENCH_SEEDS=300`. **Second CI run green** — both jobs (Typecheck+Unit+Security 1m1s · Playwright chromium-desktop 59s). Ladder assertions unchanged (medium 62.5% > easy · easy 78.3% > random · medium 94.2% > random · hard 50.0% vs medium · hard 88.3% vs random). `npm run typecheck` clean · `npm test` 1512/1512 · full suite 11.9s. **Then (same session) worked through the rest of the handoff backlog:** (1) **IP_HASH_SALT set** on Vercel Production (`vercel env add`, 32-char hex; effective next deploy) — closes the SEC-2 cross-room ipHash correlation gap. (2) **DNS** for `gdo.ax0x.ai` cannot be done via CLI — `ax0x.ai` apex is third-party-managed (not under the `panpanmao` team), so `vercel domains add` returns `domain_not_owned`. The memory's old verify token is **stale**; the real records are domain-specific (per the working sibling `gd.ax0x.ai`: a per-domain `*.vercel-dns-017.com` CNAME + a `vc-domain-verify=...,dc` TXT), generated only when the domain is added via the Vercel dashboard (Project → Settings → Domains). Steps handed to the user; still external-blocked. (3) **Manual-tribute + card-exchange interleave SHIPPED** (commit `1b1f6a5`) — closed the documented edge: `PendingTributeState.cardExchangeAfter` + extracted `openExchangeVote` helper + `finalizeManualTribute` opens the vote (instead of starting the trick) when both rules are on; `move.ts` emits `exchange_vote_required` after `tribute_resolved`. +8 tests; code-reviewer agent verified version-monotonicity + state-machine convergence + leak boundary. (4) **Bobgy Phase B attempted + empirically rejected** (commit `246f1af`) — a heuristic lead-policy (strategically-ordered decomposition lead) was implemented + benchmarked across 3 opposite-direction orderings, ALL 50.8% vs Medium (reordering the decomposition is a confirmed no-op); reverted; **Hard UI chip stays withheld** per the anti-slop gate; decisive Hard>Medium confirmed research-grade (needs determinization + multi-trick search) and documented in `docs/plan/bobgy/PHASE-B-FINDINGS.md`. Final suite **1520/1520** · typecheck clean · grep-no-leak green · CI green on `1b1f6a5`. **Remaining (genuinely external / deferred):** `gdo.ax0x.ai` DNS records (dashboard + DNS provider — user action); decisive Hard>Medium lookahead + UI chip (research-grade, deferred with empirical baseline recorded).
- 2026-05-28 (v1-completion loop — all 7 remaining milestones + 3 hardening bugs, frontier-loop /goal) — Drove the project from "playable + deployed" to "v1 feature-complete" in one autonomous loop. **Hardening (found + fixed first):** F1 `lastActiveAt` read-modify-write race → moved activity to a race-free roomStore side key (`touchActivity`/`getActivity`); cron reads `max(room.lastActiveAt, getActivity)`; unskipped the repro. F2 **AI-strength benchmark** (`benchmarks/ai-strength/` + `tests/ai/strength.test.ts`) — seeded headless self-play proving medium 62.5% > easy, easy 78% > random, medium 94% > random (the brief's "AI quality matters" claim, previously unmeasured). F13 (surfaced BY F2): `tryFullHouse` returned the first valid wildcard split instead of the maximal triple rank → move generator and `playCards` disagreed on ambiguous wildcard full houses, stalling bot turns; fixed to pick max triple rank. F3/F4: per-path coverage gate (`lib/**` 80% / `src/**` ratchet) + `mobile-modals` e2e project. **7 milestones shipped:** SEC-2 (IP throttle + salted ipHash + host-gated same-room warning + HostIPWarning), SEC-3 (profileStore + reportStore + ADMIN_TOKEN-gated admin dashboard at `#/admin` + per-opponent ReportButton), SEC-4 (BotID verdict gate on create/move/join, fail-open unknown; edge SDK activation deploy-gated), AI-3 (assist sort/suggest libs + SortButton/SuggestionHint/WildcardSubDialog/EndgameAssist wired into both tables), AI-4 (DC→bot takeover with reclaim, SSE-heartbeat liveness via race-free `seenStore`, per-minute `dcCheck` cron, PlayerStatusBadge), EXCHANGE-1 (off-by-default 换牌: `exchange.ts`/`exchangeFlow.ts` vote→select→swap state machine + 2 commands + 4 events [`exchange_completed` deal-filtered, no leak] + dealNextRound trigger + ExchangeVote/SelectModal in both tables), DEPLOY-2 (latency beacon on /move + nearest-rank p50/p95/p99 per region + AdminDashboard panel). **F12 Bobgy Phase B — honestly scoped:** Hard tier scaffolding (`lib/ai/hard.ts`: decomposition-aware following + deny-the-finisher) + dispatch `tier:'hard'` + benchmark — but the benchmark empirically confirms heuristic Hard ≈ Medium (~50%), matching the plan's "research-grade lookahead" caveat, so the **Hard UI chip is deliberately withheld** (shipping a 50% chip would be the fake-difficulty AI-slop the benchmark exists to prevent). **Delivery shape:** Wave-1 + Wave-2 parallel opus agents for disjoint-new-file milestones (SEC-3/DEPLOY-2, SEC-4, AI-3, then AI-4, SEC-2) + main-thread integration of all shared contracts (events/commands/round phase/route mounting); EXCHANGE-1 + F12 + the integration glue done main-thread for protocol safety. **Verification:** `npm run typecheck` clean · `npm test` **1512** · per-path coverage gate green (87% overall lines) · `npm run security:no-leak` green · `npm run build` 64 modules · `npm run test:e2e` 41 chromium + mobile-modals green · `scripts/ops/verify-all.sh` ✔ all green. **Remaining (non-code / deferred):** DNS records for `gdo.ax0x.ai`; Hard chip + decisive Hard>Medium lookahead (research-grade, deferred per plan); manual-tribute + exchange interleave (documented edge — exchange currently triggers on the AUTO/no-tribute path). Findings ledger: `docs/reports/frontier-loop/findings.md`.
- 2026-05-22 (audit-fix-loop end-to-end — first comprehensive multi-layer audit) — Three audit-fix rounds: parallel 4-layer audit (game/AI, realtime/API, frontend, infra) → 4 parallel opus fix agents → Round 2 verify + focused re-audit → Round 2 fix → Round 3 convergence (clear). **Findings**: 33 confidence-≥80 from Round 1 (11 CRITICAL + 19 IMPORTANT + 11 MINOR) + 6 more from Round 2 (1 CRITICAL + 3 IMPORTANT + 2 MINOR). **Critical bugs discovered and fixed**: (1) **SSE named-event dispatch silent drop** — server emits `event: deal` per `lib/realtime/sse.ts formatEvent`; client's `es.onmessage` only fires for unnamed events; every event was being silently dropped end-to-end. Caught the moment the first e2e test attempted to render the table — the entire game UI showed 0 cards. Fix: `src/lib/sseClient.ts` registers `addEventListener` for each `ServerEvent` discriminator in `SERVER_EVENT_TYPES`. Verified live via chrome-devtools (27 cards now render). (2) **Memory infra isolation in local dev** — each of the 9 Vercel route wrappers held its own `createMemoryRoomStore()`, so rooms created in `/api/room/create` were invisible to `/api/room/[code]/start`. Local dev + e2e completely broken end-to-end. Fix: `lib/realtime/sharedInfra.ts` process-singleton; all 9 routes import via `getSharedInfra()`. (3) **`canBeat` level-rank straight bug** (`lib/game/patterns.ts`) — non-bomb sequences (`straight | threePairs | twoTriples`) used `powerRank` which lifts level-rank cards to 14, so at level 5 an A-2-3-4-5 wrongly beat a 6-7-8-9-10. New private `sequenceRankValue` helper uses natural ordering. (4) **`detectTributeModeMP` 6P/8P single fallback** could route tribute from the winning team's last finisher to themselves; fix picks the lowest-positioned LOSER instead. (5) **Decomposer level-10 encoding** — `levelRank.charCodeAt(0)` returned `'1'` for level '10' but Bobgy's C++ wire format uses `'0'` for rank 10; Medium AI silently lost WASM assist at level 10 (1/13 levels). Fix: `rankToBobgyChar`. (6) **Wildcard enumeration** — bots couldn't play `pair K` with a wildcard, `triple K` with a wildcard, or 4-bomb of K with a wildcard; fix adds same-rank wildcard synthesis loop in `lib/ai/enumerate.ts`. (7) **SSE Last-Event-ID resume broken** (`lib/realtime/eventLog.ts`) — log entries used an independent per-room INCR seq; for any non-first joiner, seq didn't align with `event.version`; client reconnect missed events permanently. Fix: log id is now `event.version` directly in both memory + Upstash impls. (8) **Bot exception crashes move/start handlers** — `runBots()` throwing left idempotency reservations stranded; client retries got `move_in_flight` for 10 min, game bricked. Fix: try/catch in `runBots.ts` around `computeBotMove` + outer defense in `move.ts` + `startGame.ts`. (9) **Concurrent start race** — two simultaneous host-token POSTs both dealt independent shuffles at the same dealVersion; clients saw mixed state. Fix: `start-${code}` idempotency reservation. (10) **App.tsx passed `creds.playerId` ("p0") as `myHandle`** — `splitSeats` couldn't locate the local player, HUD showed `我 · P0`. Fix: `RoomCredentials.handle` field stored alongside credentials; App.tsx prefers `creds.handle ?? getHandle() ?? ''`. (11) **Card selection desync after own move** (`GameTable4P/MP.tsx`) — `selected` Set<number> indices survived `move_played`, pointing to wrong cards in the shrunken hand on next render. Fix: `myPlayerIdRef` + clear on own `move_played`. (12) **`vercel.json` maxDuration: 300 applied to all functions** — every endpoint inherited the SSE-only ceiling. Narrowed to SSE=300s / cron=60s / move=30s / default=15s. **Round 2 critical**: **idempotency reservation orphaned on downstream throw** — between `tryReserve` and `idempotency.commit` any throw left reservations stuck for 1h (`/start`, `/create`) or 10 min (`/move`); client retries got `*_in_flight` for that window. Fix: wrap post-reservation flow in try/catch; commit `'internal_error'` response on throw so retries get cached error replay instead of stuck pending. **New infrastructure**: `scripts/vite-api-plugin.ts` mounts all 9 Vercel route handlers as Connect middleware on the dev server — `npm run dev` and Playwright e2e share a single-origin SPA + API + SSE surface without `vercel dev`; converts Node `IncomingMessage` ↔ Web `Request/Response` (including SSE stream + abort propagation). `tests/e2e/` ships 8 spec files (health · landing · create-room · game-flow · orientation · join-flow · error-handling · full-ui-journey) + `tests/e2e/helpers/{api,ui}.ts` shared fixtures. `.github/workflows/ci.yml` runs typecheck + unit + security + e2e on PR + push to main. `scripts/ops/verify-all.sh` runs the full chain (`--no-e2e` for fast local pass). `lib/realtime/sharedInfra.ts` is the process-wide singleton for `RealtimeInfra` + the shared rate-limiter cache; dev mode multiplies rate-limit caps by `RATE_LIMIT_DEV_MULTIPLIER` (default 50) so e2e tests don't trip production-tight quotas, but production with Upstash takes the unmultiplied branch. **Test deltas**: 1022 → **1138 passing + 1 skipped** (1 documents IMPORTANT-1 lastActiveAt race repro; agent-deferred fix) + **47 e2e** (chromium-desktop + mobile-portrait + mobile-landscape; mobile projects scoped to orientation specs to avoid OrientationLock interaction flakiness on modal flows). +200 tests overall. **Verification**: `npm run typecheck` clean · `npm test` 1138/1138 · `npm run build` 248kB JS / 42kB CSS · `npm run security:no-leak` green · `npm run test:e2e` 47/47. **Files**: 70 modified · +3,633 / −436 lines. Round 3 convergence audit returned **all clear** — no new confidence-≥85 issues from Round 2's fixes (idempotency catch-and-commit, canDeclare heuristic, closeFired gate, roving-tabindex). **What this is NOT**: not a re-architecture; backend topology, AI tier ladder, tribute state machine, and event model are all unchanged. Pure correctness + robustness + a11y + observability + test coverage. Full session report at `docs/reports/audit-fix-2026-05-22/SESSION-REPORT.md`.
- 2026-05-21 (UI-7 CSS rotate + Vercel route signature fix + autofocus polish) — Three commits unblock production reachability and replace the "请横屏游戏" prompt with the intended CSS rotate UX. **`cf347e8` fix(api)**: production POST /api/room/create was returning 504 (Vercel Runtime Timeout 300s) — root cause was `export default async function handler(req): Promise<Response>` across all 9 routes. Vercel runtime treats default export as Express `(req, res) => void` and silently ignores returned Promise; cron also broke on `request.headers.get is not a function` (default-fallback passes Node IncomingMessage, not Web Request). Fix: migrated all 9 routes to named HTTP method exports (`export async function POST(request)` / `GET(request)`). Verified via `vercel dev` locally — exact user-reported payload returns 201 in 519ms (was hanging 300s). Deploy `dpl_7fqaoqai0` Ready 16s. **`2cc6fce` feat(ui)**: UI-7 CSS rotate. `src/components/OrientationLock.tsx` wraps children in `.orientation-rotate-active` (rotated 90° via CSS) when portrait-mobile; swaps to `.orientation-rotate-bypass` when text input focused (input stays mounted — unmounting closes iOS keyboard). `tokens.css` adds `--vp-h: 100dvh` root var, overridden to `100%` inside the rotate wrapper; 6 existing `100dvh` declarations migrated to `var(--vp-h, 100dvh)`. Per `docs/research/mobile-landscape-ux.md` § Update 2026-05-16 (overrode the earlier § 1.2): Majsoul / 4399 ship this pattern in production; modern iOS Safari 16+ and Chrome 90+ correctly translate pointer events through CSS transform. `RotatePrompt.tsx` retained as emergency-only fallback. **`0093e20` fix(ui)**: autofocus polish. Landing's sign-in modal auto-opens on mount when no @handle is stored; its input had `autoFocus`, which immediately triggered OrientationLock's input-bypass and hid the CSS rotate on first paint. `signInOpen` state went from boolean to `false | 'auto' | 'manual'`. Auto-open sets 'auto' (no autofocus → rotate visible); user-triggered clicks set 'manual' (autofocus on → typing immediate). **Verification**: `npm test` 1022/1022 (1016 → 1022: +4 OrientationLock + +2 Landing) · `npm run typecheck` clean · `npm run build` 244kB JS / 42kB CSS · `npm run security:no-leak` green · vercel dev curl confirms 201 response · chrome-devtools-mcp on emulated iPhone 14 Pro portrait confirms `.orientation-rotate-active` wrapper computes to `width: 844px / height: 390px / transform: rotate(90deg) / --vp-h: 100%` after the auto-modal closes.
- 2026-05-21 (Bobgy WASM Phase A delivery) — Single autonomous /goal loop took Phase A from "execution plan written" to "decomposer wired into Medium tier, deployed Ready" in one session (commit `8a41c83`). **Vendored layout**: `lib/ai/decomposer/cpp/` carries verbatim upstream C++ (`strategy.cpp` + `common.{cpp,hpp}` + `LICENSE-bobgy`) with 2 `// PATCH:` markers (flattened `cc/` include + `#include <cassert>` for newer emsdk libc++). `scripts/build-wasm.sh` runs `docker run emscripten/emsdk em++ ...` with `MODULARIZE=1 + EXPORT_ES6=1 + ENVIRONMENT='node,web' + ALLOW_MEMORY_GROWTH=1` (also patched away the deprecated `EXTRA_EXPORTED_RUNTIME_METHODS` flag in favor of `EXPORTED_RUNTIME_METHODS`). Output `dist/strategy.js` (137KB) + `dist/strategy.wasm` (111KB) committed via `.gitignore` `!lib/ai/decomposer/dist/**` exception. **TS wrapper**: `encode.ts` (Card[] → packed 2-char-per-card string, no separator — Bobgy's `parseCardState` walks 2-char chunks at `cards[i*2]/cards[i*2+1]`), `decode.ts` (solution string → DecomposerPlay[]; returns null for wildcard-substitution cases since deck-id-aware Card matching can't reverse Bobgy's hearts-of-level-rank substitutions), `loader.ts` (singleton WASM module loader via ES6 factory, `locateFile` resolves Node + Vite paths), `index.ts` (public `decomposeHand` + `preloadDecomposer`; auto-loads on first call so prod callers don't need a manual init step — first call returns null while WASM loads ~100-300ms, subsequent calls hot). **Medium integration**: endgame finisher → decomposer suggestion (when it matches an enumerated legal play via multiset cards equality on suit+rank+deck) → existing `rankByCoop` heuristic fallback. Defer cooperation policy still wins over decomposer (decomposer doesn't model coop or trick context). **Tests** (+29): `tests/ai/decomposer/{encode,decode,decompose}.test.ts` (12 + 7 + 6) + `tests/ai/medium-decomposer.test.ts` (5, separate file from `medium.test.ts` to keep WASM-preloaded vs cold paths isolated per-file; vitest isolates module state per file). 987 → 1016. **Gotchas discovered**: (1) Docker disk-full on first pull required `docker system prune -af --volumes` to reclaim 25GB; (2) `EXTRA_EXPORTED_RUNTIME_METHODS` is removed in current Emscripten — must use `EXPORTED_RUNTIME_METHODS`; (3) Bobgy's wire format for INPUT is **packed 2-char pairs with NO separator** — only the OUTPUT solutions use spaces+pipes; (4) Bobgy's solver bottoms out at a "dump all remaining cards as one `| ... |` section" fallback when it can't decompose further — that first "play" isn't a legal Guandan pattern, so medium.ts's match-against-legal-plays correctly rejects it and falls back. **Verification**: `npm test` 1016/1016 green · `npm run typecheck` clean · `npm run build` 244kB JS / 42kB CSS gzip 74kB + 8.5kB (no client-side bundle bump — WASM is server-only) · `npm run security:no-leak` green · GitHub auto-deploy `dpl_*p5ebx6y5z` Ready in 17s. Visual smoke (`npm run dev`) deferred to a manual pass; not blocking. Phase B (lookahead policy + Hard tier revival + UI chip) remains a separate future session — see `docs/plan/bobgy/PHASE-A.md §10`.
- 2026-05-21 (Bobgy WASM Phase A planning + handoff) — No code shipped beyond a planning artifact. AX redirected from "execute Phase A now" to "plan + handoff for next session" after TRIBUTE-2 shipped. Surfaced 3 critical-decision questions via AskUserQuestion; locked: (1) vendor source + commit prebuilt .wasm artifact (no CI Emscripten), (2) new `lib/ai/decomposer/` module + thin Medium hook with rule-based fallback preserved, (3) 50-turn cap for Phase A /goal. Cloned upstream `Bobgy/poker-guandan-strategy` to `/tmp/bobgy-source` for inventory: `strategy.cpp` (430 LOC, `EMSCRIPTEN_BINDINGS`) + `cc/common.{cpp,hpp}` (345 LOC total), MIT © 2018 Yuan Gong. Toolchain decision: Docker `emscripten/emsdk` rather than brew install (keeps reproducible, no per-contributor install requirement). Wrote [`docs/plan/bobgy/PHASE-A.md`](docs/plan/bobgy/PHASE-A.md) — full execution blueprint with vendor layout, build script, TS wrapper design (Node + Vite compat via `MODULARIZE=1` + `EXPORT_ES6=1` + `ENVIRONMENT='node,web'`), Medium integration shape (decomposer suggestion preferred when legal, fallback to existing `rankByCoop` heuristic when not), test strategy (`tests/ai/decomposer/{encode,decode,decompose}.test.ts` + extended `tests/ai/medium.test.ts`), 7-row risk register, 10-step checklist for next-session execution, acceptance criteria, and Phase B preview. No test/build/lint deltas this session — pure planning artifact.
- 2026-05-21 (TRIBUTE-2 6P/8P sweep tribute) — Single autonomous /goal loop closed the deferred 6P/8P sweep tribute milestone (commit `434c595`). Brings sweep through the full stack: `lib/game/tribute.ts` gains a new `TributeMode.sweep` variant + `detectTributeModeMP(mode, finishOrder, seats, hands)` for 6P/8P (sweep when winning team holds top N positions, single fallback for mixed finishes, resist takes precedence). `applyTribute` extended to accept `kind: 'sweep'` (uniform with double — both carry obligations array). `lib/game/nextRound.ts` was skipping tribute entirely for 6P/8P; now routes through `detectTributeModeMP` with same auto/manual paths as 4P. `PendingTributeState.mode` adds `'sweep'` so manual mode supports sweep obligations. `deriveTributeEvents` emits `direction: 'sweep'` for sweep modes; `TributePendingEvent.direction` + `AuthorTributePendingEvent.direction` widened to include `'sweep'`. `HandleMoveResult.tributeMode` widened; `success()` synthesizes sweep TributeMode shape. `lib/game/tributeFlow.ts finalizeManualTribute` accepts `'sweep'`. **Client**: `GameTableMP.tsx` gains tribute state in TableState + `reduceTributePending` / `reduceTributeResolved` (mirror of GameTable4P) + new `buildTributeModalState()` helper exported for tests (routes snapshot through TributeModal substates with `progressLabel` "i/N" for sweep) + `submitTributeSelect` / `submitAntiTribute` POST handlers; TributeModal renders conditionally. TributeModal.tsx unchanged — existing progressLabel handles N>2 cleanly. **Pairings** (1-indexed per `tribute-ux-deep-dive.md § Update 2026-05-17`): 6P sweep = 4→3, 5→2, 6→1; 8P sweep = 5→4, 6→3, 7→2, 8→1. **Tests**: +31 across `tributeMode.test.ts` (+8 detection), `applyTribute.test.ts` (+2 sweep finalization), `nextRound.test.ts` (+5 inc. update of pre-existing "6P skips" test), `deriveTributeEvents.test.ts` (+3 sweep events), `GameTableMP.test.tsx` (+11 reducer + buildTributeModalState). 956 → 987 tests. TS strict clean. `npm run build` clean (244kB JS / 42kB CSS · gzip 74kB + 8.5kB — modest bump from TributeModal import). grep-no-leak gate green. GitHub auto-deploy `dpl_30y921vw5` completed Ready in 18s.
- 2026-05-21 (GitHub auto-deploy unblocked) — Single commit `edeb6c7` cleared the two cloud-build issues that had forced manual `vercel deploy --prebuilt --prod` for every release. **Migration**: `scripts/migrations/add-js-extensions.py` (new, idempotent) rewrote 275 relative TS imports across 65 files in lib/ api/ src/ tests/ to add `.js` suffixes — needed because @vercel/node's per-function tsc check runs under `nodenext` moduleResolution (mandates `.js`) while our `tsconfig.app.json` uses `moduleResolution: "bundler"` (allows extensionless). Vite/Vitest with bundler resolution transparently map `.js` → `.ts` so no runtime change; both module systems are now happy. **Root tsconfig**: previously solution-style (`files: []` + references only), which meant @vercel/node walked up from the function file, found no compilerOptions, and fell back to defaults (strict OFF, no node types). That hid `process.env` (TS2591 across cron + room/create + sse routes) and broke discriminated-union narrowing in `parseBody` (TS2339 in `lib/api/createRoom.ts:65` — `parsed.error` only narrows under `strict: true`). Mirrored the tsconfig.app.json compilerOptions onto the root so the per-function check sees the same settings. **Verification**: `npm test` 956/956 green (no test deltas — purely structural); `npm run typecheck` clean; `npm run build` identical artifact (242kB JS / 42kB CSS · gzip 74kB + 8.5kB); `npm run security:no-leak` clean; `vercel build` locally now emits zero TS errors (was emitting 5+ before); post-push GitHub auto-deploy `dpl_2frvb1rst` completed Ready in 16s; curl `/api/health` returns 401 SSO page in 298ms (was hanging FUNCTION_INVOCATION_TIMEOUT for 5min before — definitive proof the function is genuinely invoking now and SSO is the only remaining gate before DNS resolves). Both prebuilt and GitHub auto-deploy paths now work; future PRs can ship via either.
- 2026-05-19 (ROOM-2 + first production deploy) — Two outstanding items from the prior session cleared. **ROOM-2**: `lib/game/mode.ts ModeRules` gains 5 new boolean axes (wildcardHeart / lastCallDeclare / steelPlate / triPair / straightFlushAboveBomb5); `lib/api/createRoom.ts parseBody` accepts a flat `BOOLEAN_RULE_KEYS` override (7 axes); `src/lib/api/rooms.ts` exports `RoomRuleOverrides`; `src/screens/CreateRoom.tsx` renames `aLevelStrict` → `strictA` (label unchanged) and threads all toggles through (only non-default values land on the wire). 24 new tests (21 mechanical contract × 7 axes via for-loop + 1 multi-override + 2 screen wire-through). Commit `a2ea2dc`. 932 → 956 tests. **Deploy**: `vercel.json` runtime field stripped (`nodejs22.x` is not a valid value; modern Vercel auto-detects Node from `.ts` extension). All 9 per-route `export const config = { runtime: 'nodejs22.x' }` blocks removed (same restriction at the per-route layer); `api/sse/[roomId].ts` keeps explicit `maxDuration: 300`. Commits `16130ce` + `2d576df`. ADMIN_TOKEN env var set on Production. First successful production deploy via `vercel deploy --prebuilt --prod` (dpl_Fjt8FhpeNrQ4cppPZdwZRneH3AZq, status Ready, all 9 functions bundled locally then uploaded) — bypassed the GitHub auto-deploy path which is currently broken (cloud per-function tsc uses nodenext moduleResolution against bundler-mode codebase, emits TS2835 errors that prevent clean function packaging on cloud rebuilds). Production aliases route to the prebuilt deploy. Custom domain `gdo.ax0x.ai` registered via Vercel API (pending DNS verification: TXT + CNAME records needed at user's DNS provider). TS strict clean. `npm run build` clean (42 modules → 242kB JS / 42kB CSS · gzip 74kB + 8.5kB). grep-no-leak gate green.
- 2026-05-19 (LLM deletion + AUTH-2 teardown + UI 3→2 tiers + doc sync) — Strategic re-decisions from HANDOFF.md 2026-05-19 backlog Groups A-D shipped. Group A: deleted `lib/ai/{hard,gateway,budget}.ts` + `lib/ai/prompts/hard.zh.{md,ts}` + their 3 test files; reverted `lib/ai/runBots.ts` to sync-only (no `runBotsAsync`); dropped `computeBotMoveAsync` from dispatch.ts; stripped `generate` + `budget` from `MoveDeps` / `StartGameDeps` + route wrappers; uninstalled `ai` SDK; cleared `AI_GATEWAY_API_KEY` + `FEATURE_AI_HARD` from `.env.example`. `RealtimeInfra.redis` exposure preserved for future adjacent clients. Group B: AUTH-2 milestone cancelled in PLAN.md; `cross-project-integration.md` marked SUPERSEDED; SUMMARY.md decisions #15 + #16 added (LLM deletion + per-app Upstash). Group C: CreateRoom.tsx + Waiting.tsx + names.ts + lifecycle.ts + rooms.ts + createRoom.ts narrowed `'easy'|'medium'|'hard'` → `'easy'|'medium'`. 970 → 917 tests (53 removed for deleted features). TS strict clean. `npm run build` clean. grep-no-leak green.
- 2026-05-18 (env-aliasing + AI Gateway + createUpstashBudget + TRIBUTE-1 part E session, 3 commits) — Production AI Gateway wiring shipped via `generateText('deepseek/deepseek-chat')` from `ai@6`; route wrappers `api/room/[code]/{move,start}.ts` inject the real generate fn when `AI_GATEWAY_API_KEY` is set, else silent Medium fallback. `createUpstashBudget(redis, now?)` shipped with `ai:budget:YYYY-MM` (UTC) keys; route wrappers select Upstash vs memory from `infra.redis`. Upstash env vars aliased so both `UPSTASH_REDIS_REST_*` and `KV_REST_API_*` (Vercel Marketplace naming) work — UPSTASH-prefixed wins. TRIBUTE-1 part E manual `tribute_select` + `anti_tribute` commands now dispatch through new `lib/game/tributeFlow.ts` pure transitions (auto-mode at round-start unchanged — `pendingTribute` setting is a future phase). Vercel project `panpanmao/guandan-online` linked this session (separate from sibling scorer). 922 → 970 tests. `npm build` clean. (Hard tier + AI Gateway + budget all removed on 2026-05-19; see above entry.)
- 2026-05-18 (UI-3/4/5/6 + bot-dispatch session, 6 commits) — All remaining UI tracks shipped. UI-3 (Landing+CreateRoom+Waiting+rooms API client+identity+router, +57 tests). Bot dispatch (`lib/ai/runBots.ts` wired into `lib/api/move.ts`, +8 tests; auto-calls `startTrick` between tricks). UI-4 (TributeModal across 4 substates, +10 tests). UI-5 (LevelLadder + RoundEnd + ALevelFinal + Victory, +18 tests). UI-6 (seating.ts + GameTableMP shared 6P/8P table + App TableSwitch wrapper, +19 tests). Final fixture-type fix for vite build's strict tsc. Visual smoke on iPhone 14 Pro landscape confirms Landing + CreateRoom match demos S01/S02. 768 → 880 tests. `pnpm build` clean: 233kB JS / 41kB CSS (gzip 72kB + 8.5kB).
- 2026-05-18 (UI + AI track session, 4 more commits) — UI foundation + AI tier ladder shipped. UI-1 (`5d2f959`) ports demos CSS to `src/styles/{tokens,components}.css` + adds Card/Hand/Trick/Avatar React primitives with 30 component tests (jsdom + @testing-library/react). UI-2 (`2436f87`) wires OrientationLock + RotatePrompt (rotate-prompt fallback per mobile-landscape research, NOT CSS-transform-rotate) + GameTable4P live-SSE screen with `sseClient.ts` and pure `reduceEvent()` reducer. AI-1 finish (`204588d`) adds `coop.ts` (partner cooperation: defer/cover/compete), `medium.ts` (Medium tier with no noise), `dispatch.ts` (single `computeBotMove()` entry point). AI-2 (`e194a80`) adds Hard tier via Vercel AI Gateway with 5 silent-fallback triggers + budget guardrail (soft $50, hard $100); LLM call dependency-injected via `ctx.generate` so tests mock without SDK. Outstanding: UI-3/4/5/6, AUTH-2 (Critical Decision), bot wire-up in move handler. 676 → 768 tests.
- 2026-05-18 (backend-completion session, 3 more commits) — Backend feature-complete. SESSION-1 (`45222be`) ships sessionStore + round_end/game_end events from the move handler. LIFECYCLE-1 (`95b4ff4`) wires room_joined/room_left fanout with shared RoomState.eventVersion counter — per-recipient SSE log seq now contiguous across lobby → game boundary. CRON-1 (`2b6fb4e`) ships the stale-room cleanup endpoint with active-codes index + Vercel hourly cron. 629 → 676 tests.
- 2026-05-18 (extension session, 7 more commits) — Backend wire-complete. publishEvent fanout from move handler (API-4 part B), POST `/api/room/[code]/start` (API-6), end-to-end integration test (INT-1), per-recipient EventLog keys closing the SSE backlog leak (SEC-2), GET `/api/room/[code]` public view (API-7), trick_won event derivation with multi-event move versioning (EVT-1). 590 → 629 tests.
- 2026-05-18 (first session, 9 commits) — NET-1C live Upstash impls (idempotency / eventLog / eventBus / realtime-infra factory) + API-1 through API-3 (roomStore + create + join + leave + move handler without publishEvent + SSE handler). 487 → 590 tests.
- 2026-05-18 (kickoff session, 25 commits) — P0 logic layer + most of P1 logic. Modules in `lib/`: game (10 patterns + state machine + session + tribute + hand sort), realtime (15 events + 6 commands + sse + bus + log + idempotency + filter + publish gateway + card codec + handleMove dispatcher), room (code + lifecycle), ai (full enumerator + Easy bot + timing + names), security (rate limiter). 487/487 tests, TS strict clean. See `HANDOFF.md` for commit-by-commit map.
- 2026-05-17 — P0 kickoff: bootstrap + AUTH-1 + CORE-1 part 1 shipped (3 commits). 111/111 tests, 98.45% statement coverage.
- 2026-05-16 — Initial scaffold (research phase begin)
